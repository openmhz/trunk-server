apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: certbot-claim0
  name: certbot-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
